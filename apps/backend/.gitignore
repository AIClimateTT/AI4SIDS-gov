__pycache__/
*.pyc
.venv/
dev.db
.env

# Raw survey exports contain PII (names, phone numbers, ID numbers) and must
# never be committed. Only fixtures/sample_small.csv (hand-crafted, no real
# PII) is meant to be tracked.
output.csv
*.raw.csv
